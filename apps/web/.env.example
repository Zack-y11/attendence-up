# Clerk Dashboard → your app → API keys.
# Local development uses the Development key (pk_test_...).
# Vercel production uses the Production key (pk_live_...).
# Never put the secret key (sk_...) in this file. The browser bundle would expose it.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
